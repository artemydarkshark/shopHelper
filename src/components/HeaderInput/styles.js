import { StyleSheet } from "react-native";

export default StyleSheet.create({
  containerInput: {
    width: 110,
    position: "relative"
  },
  input: {
    color: "#fff",
    height: 35,
    fontSize: 15
  },
  icon: {
    position: "absolute",
    top: 10,
    right: 0,
    color: "#fff"
  }
});
