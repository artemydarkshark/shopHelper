import { StyleSheet } from "react-native";

export default StyleSheet.create({
  inputContainer: {
    marginBottom: 18
  },
  input: {
    paddingHorizontal: 10,
    paddingVertical: 0,
    height: 30
  }
});
